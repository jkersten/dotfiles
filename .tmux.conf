set -g prefix C-a
unbind C-b
bind C-a send-prefix

unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer
bind -t vi-copy 'v' begin-selection
bind -t vi-copy 'y' copy-selection

bind C-s set-window-option synchronize-panes

set -g default-terminal "screen-256color"
set -g history-limit 5000

setw -g mode-keys vi

set -g default-command /bin/zsh
set -g default-shell /bin/zsh

set -g utf8
setw -g utf8 on

set -sg escape-time 1
set -g repeat-time 1000

set -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1

setw -g mode-mouse on

bind r source-file ~/.tmux.conf \; display "Reloaded!"

unbind %
unbind '"'
bind | split-window -h
bind - split-window -v

#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R

# smart pane switching with awareness of vim splits
bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-l) || tmux select-pane -R"
bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys 'C-\\') || tmux select-pane -l"

bind T swap-window -t 1

unbind ^J
bind ^J select-pane -t :.+

bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

#set -g status-fg white
#set -g status-bg colour235

#setw -g window-status-fg colour135
#setw -g window-status-bg colour235
##setw -g window-status-attr dim

#setw -g window-status-current-fg colour235
#setw -g window-status-current-bg colour155
#setw -g window-status-current-attr dim

#set -g pane-border-fg colour155
#set -g pane-border-bg default
#set -g pane-active-border-fg colour155
#set -g pane-active-border-bg colour155

#set -g message-fg black
#set -g message-bg colour155
#set -g message-attr dim

set -g status-utf8 on
set -g status-interval 15
set -g status-justify left
set -g status-left-length 40
set -g status-right-length 80
# set -g status-left "#[bg=colour235]#[fg=colour155]#S #[fg=default]#I:#P"
#set -g status-right "#[fg=colour143]#(whoami)#[fg=default] at #[fg=colour125]#h #[fg=colour155]%d %b %R #[fg=cyan]#(~/battery charging) #[fg=default]#(cat ~/.pomo_stat)"

#set -g status-left "#[bg=black]#[fg=magenta]#S #[fg=cyan]#I:#P"
set -g status-left "#[fg=colour16,bg=colour254,bold]#[fg=magenta]#S #[fg=cyan]#I:#P"
set -g status-right "#[fg=yellow]#(whoami)#[fg=colour240] at #[fg=blue]#h #[fg=magenta]%d %b %R #[fg=cyan]#(~/battery charging) #[fg=default]#(cat ~/.pomo_stat)"

setw -g monitor-activity on
set -g visual-activity on

#### COLOUR (Solarized dark)

# default statusbar colors
set-option -g status-bg black #base02
set-option -g status-fg yellow #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg brightblue #base0
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg brightred #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg black #base02
set-option -g pane-active-border-fg brightgreen #base01

# message text
set-option -g message-bg black #base02
set-option -g message-fg brightred #orange

# pane number display
set-option -g display-panes-active-colour blue #blue
set-option -g display-panes-colour brightred #orange

# clock
set-window-option -g clock-mode-colour green #green



#if-shell 'test -z "$POWERLINE_COMMAND"' 'if-shell "which powerline-client" "set-environment -g POWERLINE_COMMAND powerline-client" "set-environment -g POWERLINE_COMMAND powerline"'
#set -g status-fg colour231
#set -g status-bg colour234
#set -g status-left-length 20
#set -g status-left '#[fg=colour16,bg=colour254,bold] #S #[fg=colour254,bg=colour234,nobold]#(eval $POWERLINE_COMMAND tmux left)'
#set -g status-right '#(eval $POWERLINE_COMMAND tmux right -R pane_id=`tmux display -p "#D"`)'
#set -g status-right-length 150
#set -g window-status-format "#[fg=colour244,bg=colour234]#I #[fg=colour240] #[default]#W "
#set -g window-status-current-format "#[fg=colour234,bg=colour31]#[fg=colour117,bg=colour31] #I  #[fg=colour231,bold]#W #[fg=colour31,bg=colour234,nobold]"
#set-window-option -g window-status-fg colour249
#set-window-option -g window-status-activity-attr none
#set-window-option -g window-status-bell-attr none
#set-window-option -g window-status-activity-fg yellow
#set-window-option -g window-status-bell-fg red
# vim: ft=tmux



